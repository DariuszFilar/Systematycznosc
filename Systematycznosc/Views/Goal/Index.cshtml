@model Systematycznosc.ViewModels.GoalViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    <style>
        body {
            background-color: #1abc9c
        }
    </style>
}


<div class=" d-flex align-items-center flex-column portfolio bg-primary text-white mb-0">

    <!-- Masthead Avatar Image -->
    <!-- Masthead Heading -->
    <h1 class="masthead-heading text-uppercase mb-0" style="font-size:55px; text-align:center;" id="Opener">Cele dzienne</h1>


    <!-- Icon Divider -->
    <div class="divider-custom divider-light" style="">
        <div class="divider-custom-line"></div>
        <div class="divider-custom-icon">
            <i class="glyphicon glyphicon-star" style="font-size: 55px;"></i>
        </div>
        <div class="divider-custom-line"></div>
    </div>
    <div style="font-size:37px; text-align:center; padding-bottom:50px;">Warto je spełniać</div>

    @{ // TODO: Make validation if can go forward
        {
            using (Html.BeginForm("Index", "Goal", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal col-md-11 col-s-5 col-lg-8", role = "form", style = " text-align: center; color:black; font-size:25px" }))
            {
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                {
                    <div class="form-group">
                        <div class="col-md-10" style="margin-left: auto; margin-right: auto; float: none!important;">
                            @Html.Label("Nazwa celu:", new { @class = "control-label", style = "margin-left:auto; margin-right:auto; display:inline-block; float:none;" })
                            @Html.TextBox("goalName", null, new { placeholder = "Tu wpisz pytanie odnośnie celu", @class = "form-control", style = "float:none!important; display:inline-block; font-size:25px;", name = "relationshipValue", required = string.Empty })
                            @Html.Label("Pytanie odnośnie celu:", new { @class = "control-label", style = "margin-left:auto; margin-right:auto; display:inline-block; float:none;" })
                            @Html.TextBox("goalQuestion", null, new { placeholder = "Tu wpisz pytanie odnośnie celu", @class = "form-control", style = "float:none!important; display:inline-block; font-size:25px;", name = "relationshipValue", required = string.Empty })
                        </div>
                    </div>
                }
                <div style="padding-bottom:50px; padding-top:20px;">
                    <div style=" padding-bottom:3px; padding-left:3px; padding-right:3px; padding-top:3px; display:inline-block; margin-bottom:10px;">
                        <button style="font-size:20px; color:white;" type="submit" value="newGoal" class="btn btn-xl btn-outline-info btn-secondary" id="saveButton" name="savebutton">Zapisz</button>
                    </div>
                    <div style="padding-bottom:3px; padding-left:3px; padding-right:3px; display:inline-block; margin-bottom:10px;" id="panel2">
                        @Html.ActionLink("Panel edycyjny", "RelationshipEdit", "Relationship", null, new { @class = "btn btn-xl btn-outline-info btn-secondary", style = "font-size:20px; color:white;" })
                    </div>
                </div>
            }
        }
        //else
        //{
        //    <div style="font-size:25px; color:black;" class="mr-1">Osiągnięto maksymalną ilość Credo, wejdź w opcje by edytować</div>
        //}
    }
    <div style="padding-bottom:25px; padding-left:3px; padding-right:3px; display:inline-block; margin-bottom:10px; padding-top:10px;" id="panel">
        @Html.ActionLink("Panel edycyjny", "RelationshipEdit", "Relationship", null, new { @class = "btn btn-xl btn-outline-info btn-secondary", style = "font-size:20px; color:white;" })
    </div>
</div>
<div class=" d-flex align-items-center flex-column" style="background-color:white!important;">
    @{
        if (Model.FirstGoals.Count > 2)
        {
            if (Model.FirstGoals[0].GoalName != null)
            {
                <h1 class="masthead-heading text-uppercase mb-0" style="font-size:55px; padding-top:55px;text-align:center; color:black!important;">@Model.FirstGoals[0].GoalName</h1>
                <!-- Icon Divider -->
                <div class="divider-custom">
                    <div class="divider-custom-line"></div>
                    <div class="divider-custom-icon">
                        <i class="glyphicon glyphicon-star" style="font-size: 55px;"></i>
                    </div>
                    <div class="divider-custom-line"></div>
                </div>
                <div style="font-size:37px; text-align:center; padding-bottom:50px;">@Model.FirstGoals[0].GoalQuestion</div>
                using (Ajax.BeginForm("Index", "Goal", new { ReturnUrl = ViewBag.ReturnUrl, UpdateTargetId = "FirstGoalTable" }, new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "FirstGoalTable" }, new { @class = "form-horizontal col-md-11 col-s-5 col-lg-8", role = "form", style = " text-align: center; color:black; font-size:25px" }))
                {
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div>
                        @Html.RadioButton("status", "Tak", new { name = "", id = "" })@Html.Label("Tak")
                        @Html.RadioButton("status", "Nie", new { name = "", id = "" })@Html.Label("Nie")
                    </div>
                    <div>
                        @Html.RadioButton("status", "Odpoczynek", new { name = "", id = "" })@Html.Label("Odpoczynek")
                        @Html.RadioButton("status", "Nagroda", new { name = "", id = "" })@Html.Label("Nagroda")
                    </div>
                    <div style=" padding-bottom:3px; padding-left:3px; padding-right:3px; padding-top:3px; display:inline-block; margin-bottom:10px;">
                        <button style="font-size:40px;" type="submit" value="firstGoal" class="btn btn-success" name="saveButton" id="saveButton">Zapisz</button>
                    </div>
                }
                <div id="FirstGoalTable">@{Html.RenderPartial("_FirstGoalTable", Model);}</div>
            }
        }
    }
</div>
<div class=" d-flex align-items-center flex-column">
    @{
        if (Model.SecondGoals.Count > 2)
        {
            if (Model.SecondGoals[0].GoalName != null)
            {
                <h1 class="masthead-heading text-uppercase mb-0" style="font-size:55px; padding-top:55px;text-align:center; color:black!important;">@Model.SecondGoals[0].GoalName</h1>
                <!-- Icon Divider -->
                <div class="divider-custom">
                    <div class="divider-custom-line"></div>
                    <div class="divider-custom-icon">
                        <i class="glyphicon glyphicon-star" style="font-size: 55px;"></i>
                    </div>
                    <div class="divider-custom-line"></div>
                </div>
                <div style="font-size:37px; text-align:center; padding-bottom:50px;">@Model.SecondGoals[0].GoalQuestion</div>
                using (Ajax.BeginForm("Index", "Goal", new { ReturnUrl = ViewBag.ReturnUrl, UpdateTargetId = "SecondGoalTable" }, new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "SecondGoalTable" }, new { @class = "form-horizontal col-md-11 col-s-5 col-lg-8", role = "form", style = " text-align: center; color:black; font-size:25px" }))
                {
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div>
                        @Html.RadioButton("status", "Tak", new { name = "", id = "" })@Html.Label("Tak")
                        @Html.RadioButton("status", "Nie", new { name = "", id = "" })@Html.Label("Nie")
                    </div>
                    <div>
                        @Html.RadioButton("status", "Odpoczynek", new { name = "", id = "" })@Html.Label("Odpoczynek")
                        @Html.RadioButton("status", "Nagroda", new { name = "", id = "" })@Html.Label("Nagroda")
                    </div>
                    <div style=" padding-bottom:3px; padding-left:3px; padding-right:3px; padding-top:3px; display:inline-block; margin-bottom:10px;">
                        <button style="font-size:40px;" type="submit" value="secondGoal" class="btn btn-success" name="saveButton" id="saveButton">Zapisz</button>
                    </div>
                }
                <div id="SecondGoalTable">@{Html.RenderPartial("_SecondGoalTable", Model);}</div>
            }
        }
    }
</div>

<div class=" d-flex align-items-center flex-column">
    @{
        if (Model.ThirdGoals.Count > 2)
        {
            if (Model.ThirdGoals[0].GoalName != null)
            {
                <h1 class="masthead-heading text-uppercase mb-0" style="font-size:55px; padding-top:55px;text-align:center; color:black!important;">@Model.ThirdGoals[0].GoalName</h1>
                <!-- Icon Divider -->
                <div class="divider-custom">
                    <div class="divider-custom-line"></div>
                    <div class="divider-custom-icon">
                        <i class="glyphicon glyphicon-star" style="font-size: 55px;"></i>
                    </div>
                    <div class="divider-custom-line"></div>
                </div>
                <div style="font-size:37px; text-align:center; padding-bottom:50px;">@Model.ThirdGoals[0].GoalQuestion</div>
                using (Ajax.BeginForm("Index", "Goal", new { ReturnUrl = ViewBag.ReturnUrl, UpdateTargetId = "ThirdGoalTable" }, new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "SecondGoalTable" }, new { @class = "form-horizontal col-md-11 col-s-5 col-lg-8", role = "form", style = " text-align: center; color:black; font-size:25px" }))
                {
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div>
                        @Html.RadioButton("status", "Tak", new { name = "", id = "" })@Html.Label("Tak")
                        @Html.RadioButton("status", "Nie", new { name = "", id = "" })@Html.Label("Nie")
                    </div>
                    <div>
                        @Html.RadioButton("status", "Odpoczynek", new { name = "", id = "" })@Html.Label("Odpoczynek")
                        @Html.RadioButton("status", "Nagroda", new { name = "", id = "" })@Html.Label("Nagroda")
                    </div>
                    <div style=" padding-bottom:3px; padding-left:3px; padding-right:3px; padding-top:3px; display:inline-block; margin-bottom:10px;">
                        <button style="font-size:40px;" type="submit" value="secondGoal" class="btn btn-success" name="saveButton" id="saveButton">Zapisz</button>
                    </div>
                }
                <div id="ThirdGoalTable">@{Html.RenderPartial("_ThirdGoalTable", Model);}</div>
            }
        }
    }
</div>

@section Scripts
{
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.20/css/jquery.dataTables.css">
    <script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/1.10.20/js/jquery.dataTables.js"></script>
    <script src="//cdn.datatables.net/plug-ins/1.10.11/sorting/date-eu.js" type="text/javascript"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/moment.js/2.8.4/moment.min.js"></script>
    <script src="//cdn.datatables.net/plug-ins/1.10.12/sorting/datetime-moment.js"></script>
    <script src="//cdn.datatables.net/plug-ins/1.10.11/sorting/date-eu.js"></script>
    <script src="~/scripts/jquery-1.10.2.js"></script>
    <script src="~/scripts/jquery.unobtrusive-ajax.js"></script>
}
@section Scripts
{
    <script src="~/scripts/jquery.unobtrusive-ajax.js"></script>
}

@Scripts.Render("~/bundles/jquery")
<script type="text/javascript">
    var amountOfCredo = @Model.Credos.Count();

    $(document).ready(function () {
        if (@Model.Credos.Count() <= 11) {
            $("#panel").hide();
        }
         if (@Model.Credos.Count() == 0) {
            $("#panel2").hide();
        }
    });

  $('#saveButton').click(function (e) {
        $.ajax({
            success: function (data) {
                amountOfCredo++;
                if (amountOfCredo > 11) {
                    $("#addform").html("Osiągnięto maksymalną ilość Credo, wejdź w opcje by edytować");
                    $("#panel").show();
                }
                if (amountOfCredo > 0) {
                    $("#panel2").show();
                }
            },
        });
    });
</script>
